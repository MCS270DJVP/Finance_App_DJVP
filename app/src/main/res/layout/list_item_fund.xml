<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
             android:layout_width="match_parent"
             android:layout_height="wrap_content">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/list_item_ticker_textview"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:textSize="30sp"
            android:textStyle="bold"/>

        <!--Testing: display the price on the internet-->

        <TextView
            android:id="@+id/price_display"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_marginTop="5dp"
            android:layout_alignBaseline="@+id/list_item_ticker_textview"
            android:paddingRight="5dp"
            android:textSize="24sp"/>

        <ImageButton
            android:id="@+id/remove"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:hint="Delete"
            android:src="@drawable/ic_remove_button"/>

        <Spinner
            android:id="@+id/list_item_weight_spinner"
            android:layout_width="145dp"
            android:layout_height="match_parent"
            android:layout_marginTop="3dp"
            android:layout_gravity="top|right"
            android:background="@android:drawable/btn_dropdown"
            android:entries="@array/list_item_weight_spinner"
            android:layout_alignBaseline="@+id/remove"
            android:spinnerMode="dropdown">
        </Spinner>

    </LinearLayout>

</FrameLayout>